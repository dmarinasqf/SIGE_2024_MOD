@if (User.IsInRole("ADMINISTRADOR") || User.IsInRole("MENU HORARIOS"))
{
<li class="nav-item">
    <a href="#" class="nav-link dropdown-toggle collapsed">
        <i class="nav-icon fa fa-cube"></i>
        <span class="nav-text fadeable">
            <span>Depositos</span>
        </span>
        <b class="caret fa fa-angle-left rt-n90"></b>
    </a>


    <div class="hideable submenu collapse">
        <ul class="submenu-inner">
            @if (User.IsInRole("ADMINISTRADOR") || User.IsInRole("VER DEPOSTIO"))
            {
                <li class="nav-item">
                    <a class="nav-link" asp-area="Finanzas" asp-controller="FPagos" asp-action="ValidarDeposito">Validar Deposito</a>
                </li>
            }
            @if (User.IsInRole("ADMINISTRADOR") || User.IsInRole("REPORTE DEPOSITO"))
            {
                <li class="nav-item">
                    <a class="nav-link" asp-area="Finanzas" asp-controller="FPagos" asp-action="ReporteDeposito">Reporte</a>
                </li>
            }
        </ul>
    </div>

    <b class="sub-arrow"></b>

</li>
}